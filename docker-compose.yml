services:
  api:
    build:
      context: ./apps/api
    ports:
      - "8080:8080"
    env_file:
      - ./apps/api/.env
    restart: unless-stopped

  web:
    build:
      context: ./apps/web
    ports:
      - "3000:3000"
    env_file:
      - ./apps/web/.env
    environment:
      - NEXT_PUBLIC_API_URL=http://api:8080
    restart: unless-stopped
    depends_on:
      - api
